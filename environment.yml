name: cornell_course_roster
channels:
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free
  - defaults
  # - conda-forge
dependencies:
  - python=3.11 # You can specify your desired Python version here
  - scrapy
  - requests
  - beautifulsoup4
  - lxml
  - pandas
  - sqlalchemy
  - notebook # To support Jupyter notebooks
  - ipykernel # To enable IPython kernel in Jupyter
  - pip
  - pip:
      - praw

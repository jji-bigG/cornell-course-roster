# takes the parsed output and converts that in memory data structure into a csv file
# the format of the file is according to the dictory keys in the data structure
# one file for every semester for easier data processing later on
